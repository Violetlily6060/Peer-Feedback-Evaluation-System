package domain;
public class Administrator extends User {

    public Administrator(String userID, String password, String name, String email, String phoneNo) {
        super(userID, password, name, email, phoneNo);
    }
}
